use git2::Repository;

pub fn grasp(repo: Repository) -> anyhow::Result<()> {
    /* TODO
    ensure repo is clean
    checkout default
    fetch
    rebase default on origin/default
    checkout -
    rebase on default
     */
    todo!()
}